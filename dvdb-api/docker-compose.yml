version: "3"
services:
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=museo-db
    ports:
      - "5432:5432"
    volumes:
      - PGDATA:/var/lib/postgresql/data
volumes:
  PGDATA:
